import 'package:flutter/material.dart';

class CarouselScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Carousel Details")),
      body: Center(child: Text("Details about Carousel Items")),
    );
  }
}